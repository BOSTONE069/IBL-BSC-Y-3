# Prompt the user for input and store the result in a variable called input_str
user_text = input("Please enter a string: ")

# Replace all occurrences of a space character with three periods and store the result in a variable called output_str
output_str = user_text.replace(" ", "...")

# Output the modified string to the console
print(output_str)
